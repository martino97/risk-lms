# Django Settings
SECRET_KEY=your-secret-key-here-generate-a-long-random-string-change-this-in-production
DEBUG=True
ALLOWED_HOSTS=localhost,127.0.0.1

# Database Configuration
DB_NAME=risk_lms
DB_USER=postgres
DB_PASSWORD=your_database_password_here
DB_HOST=localhost
DB_PORT=5432

# Media Files
MEDIA_ROOT=media/
MEDIA_URL=/media/

# Static Files
STATIC_ROOT=staticfiles/
STATIC_URL=/static/

# FFmpeg Path (if not in system PATH)
# FFMPEG_PATH=C:/ffmpeg/bin/ffmpeg.exe

# Email Configuration (for notifications)
EMAIL_BACKEND=django.core.mail.backends.console.EmailBackend
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USE_TLS=True
EMAIL_HOST_USER=your-email@gmail.com
EMAIL_HOST_PASSWORD=your-app-password

# Redis Configuration (for Celery background tasks)
REDIS_URL=redis://localhost:6379/0

# Domain (for certificate verification URLs)
DOMAIN=http://localhost:8000

# File Upload Limits (in bytes)
MAX_VIDEO_SIZE=2147483648  # 2GB
MAX_IMAGE_SIZE=10485760    # 10MB

# Security Settings (for production)
# SESSION_COOKIE_SECURE=True
# CSRF_COOKIE_SECURE=True
# SECURE_SSL_REDIRECT=True
